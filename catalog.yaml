sources:
  EU:
    description: EU Node  
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/EU/main.yaml"
  NERSC:
    description: NERSC Node  
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/NERSC/main.yaml"
  NCAR:
    description: NCAR Node  
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/NCAR/main.yaml"
  online:
    description: online data sources
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/online/main.yaml"

